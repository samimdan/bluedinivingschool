
import '../gsap/index.js';
class MigleardAnimation {
    constructor(elements = [], toOptions = [], setOptions = [], fromOptions = [], options = []) {
        this.name = 'MigleardAnimation';
        this.elements = [...elements];
        this.options = [...toOptions];
        this.setOptions = [...setOptions];
        this.fromOptions = [...fromOptions];
        this.options = [...options];

    }
    fillobject() {
        console.log("fill object");
        // const selectedElements = document.querySelectorAll(this.elements);
        // selectedElements.forEach((elements) => {
        //     elements.style = this.options;
        // });
    }
}
export default MigleardAnimation;